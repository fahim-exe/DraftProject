#Run this visual code to get the output.
#Before running the code the directory must be set, otherwise it may show error

Steps to set the directory:

1. Go to your C drive
2. Find "Users" forlder
3. Find your user name (it may sometime saved ase "User") and remember it (must maintain the Upper and lower letters)
4. Go to form-1 and form-2 of the the code (Form1.cs, Form2.cs)
5. Change the part of -1 and -2 (instructions are added in comments)


If the directory is set correctly the code will run without any bugs.
